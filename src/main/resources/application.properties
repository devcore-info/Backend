# Spring Boot Configuration
server.port=8080

# Database Configuration (Microsoft SQL Server)
spring.datasource.url=jdbc:sqlserver://${DB_HOST:localhost}:1433;databaseName=ConnextionDB;trustServerCertificate=true
spring.datasource.username=sa
spring.datasource.password=16200122wqkj
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Enable SQL script initialization (schema.sql and data.sql)
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=classpath:data.sql

# Logging level
logging.level.org.springframework.web=INFO
logging.level.org.springframework.jdbc=DEBUG
